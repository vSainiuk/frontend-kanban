import { HEIGHT } from '@/constants/height-elements.constants'

export default function Header() {
	return <header className='bg-card rounded-b-2xl p-2' style={{ height: `${HEIGHT.header}` }}></header>
}
